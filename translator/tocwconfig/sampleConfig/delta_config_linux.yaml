exporters:
    telegraf_cloudwatch: {}
receivers:
    telegraf_diskio: {}
service:
    pipelines:
        metrics:
            exporters:
                - telegraf_cloudwatch
            processors: []
            receivers:
                - telegraf_diskio
