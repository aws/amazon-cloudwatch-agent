exclude:
  match_type: regexp
  attributes:
    - { key: "^cloud.*" }
    - { key: "^host.*" }
    - { key: "^os.*" }
    - { key: "^process.*" }
    - { key: "^service.*" }
    - { key: "^telemetry.*" }
  actions:
    - key: "^cloud.*"
      action: delete
    - key: "^host.*"
      action: delete
    - key: "^os.*"
      action: delete
    - key: "^process.*"
      action: delete
    - key: "^service.*"
      action: delete
    - key: "^telemetry.*"
      action: delete