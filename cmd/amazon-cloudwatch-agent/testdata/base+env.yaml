receivers:
  nop:
  nop/1:

exporters:
  nop:

extensions:
  nop:

service:
  extensions: [nop]
  pipelines:
    metrics:
      receivers: [nop]
      exporters: [nop]
    traces:
      receivers: [nop/1]
      exporters: [nop]
