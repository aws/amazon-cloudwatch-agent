# Minimal CloudWatch Agent OpAMP Supervisor Configuration

server:
  endpoint: wss://opamp.monitoring.amazonaws.com/v1/opamp

capabilities:
  reports_effective_config: true
  reports_health: true
  accepts_remote_config: true
  reports_remote_config: true

agent:
  executable: /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent
  config_files:
    - /opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.yaml

storage:
  directory: ./storage